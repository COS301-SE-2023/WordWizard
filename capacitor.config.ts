import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.umleiten.wordWizard',
  appName: 'Word Wizard',
  webDir: './dist/apps/word-wizard',
};

export default config;
