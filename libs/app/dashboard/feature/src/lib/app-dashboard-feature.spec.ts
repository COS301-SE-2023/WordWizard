import { appDashboardFeature } from './app-dashboard-feature';

describe('appDashboardFeature', () => {
  it('should work', () => {
    expect(appDashboardFeature()).toEqual('app-dashboard-feature');
  });
});
