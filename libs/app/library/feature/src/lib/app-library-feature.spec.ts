import { appLibraryFeature } from './app-library-feature';

describe('appLibraryFeature', () => {
  it('should work', () => {
    expect(appLibraryFeature()).toEqual('app-library-feature');
  });
});
