import { appUserAgreementFeature } from './app-user-agreement-feature';

describe('appUserAgreementFeature', () => {
  it('should work', () => {
    expect(appUserAgreementFeature()).toEqual('app-user-agreement-feature');
  });
});
