import { appAchievementsDataAccess } from './app-achievements-data-access';

describe('appAchievementsDataAccess', () => {
  it('should work', () => {
    expect(appAchievementsDataAccess()).toEqual('app-achievements-data-access');
  });
});
