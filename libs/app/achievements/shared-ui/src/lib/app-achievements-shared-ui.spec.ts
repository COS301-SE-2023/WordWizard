import { appAchievementsSharedUi } from './app-achievements-shared-ui';

describe('appAchievementsSharedUi', () => {
  it('should work', () => {
    expect(appAchievementsSharedUi()).toEqual('app-achievements-shared-ui');
  });
});
