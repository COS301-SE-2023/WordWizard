import { appAchievementsFeature } from './app-achievements-feature';

describe('appAchievementsFeature', () => {
  it('should work', () => {
    expect(appAchievementsFeature()).toEqual('app-achievements-feature');
  });
});
